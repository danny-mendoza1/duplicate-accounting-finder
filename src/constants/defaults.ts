import { DEFAULT_PRESET } from '../types';

// TODO: remove this to only use the default preset
export const CSV_COLS = DEFAULT_PRESET.csvColumns;
export const JSON_COLS = DEFAULT_PRESET.jsonColumns;
